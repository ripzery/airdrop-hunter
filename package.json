{
  "name": "airdrop-hunter",
  "version": "1.0.0",
  "description": "Hunt for potential airdrop project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "start": "npx ts-node src/index.ts",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "deploy:testnet": "cd src/contracts && truffle migrate --network testnet && cdq ../..",
    "deploy:prod": "cd src/contracts && truffle migrate --network bsc && cd ../.."
  },
  "keywords": [
    "defi",
    "airdrop"
  ],
  "author": "Phuchit Sirimongkolsathien",
  "license": "ISC",
  "dependencies": {
    "@dydxprotocol/solo": "^0.39.2",
    "axios": "^0.21.1",
    "bignumber.js": "^9.0.1",
    "chalk": "^4.1.0",
    "dotenv": "^8.2.0",
    "web3": "^1.3.0",
    "web3-utils": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.24",
    "gts": "^3.0.3",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3"
  }
}
